import firestore from '@react-native-firebase/firestore';
import { TYPE_COLLECTIONS } from './constants';
import { formatResponse } from './scripts';

export {firestore as fsdb, TYPE_COLLECTIONS, formatResponse}